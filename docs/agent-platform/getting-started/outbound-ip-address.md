# (In)Agent Platform Outbound IP Addresses

(In)Agent Platform makes outbound requests from a fixed set of IP addresses. If your systems or third-party services enforce IP allowlisting, you must add `74.162.106.212` IP to your firewall or network's allowlist. This action ensures that traffic originating from (In)Agent Platform is recognized and permitted by your network or external service endpoints.
